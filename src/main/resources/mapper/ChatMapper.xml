<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mizoguchi.misaki.mapper.ChatMapper">
    <select id="searchChats" resultType="org.mizoguchi.misaki.pojo.entity.Chat">
        SELECT DISTINCT c.*
        FROM chat c
        LEFT JOIN message m ON c.id = m.chat_id
        WHERE c.user_id = #{userId}
          AND c.delete_flag = 0
          AND (
            c.title LIKE CONCAT('%', #{keyword}, '%')
                OR m.content LIKE CONCAT('%', #{keyword}, '%')
            )
        ORDER BY c.update_time DESC
    </select>
</mapper>